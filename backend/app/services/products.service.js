const mongoose = require('mongoose');

const consola = require('consola');

class ProductsService {
	constructor() {}

	async findAll() {
		try {
		} catch (error) {}
	}

	async findById(pruductId) {
		try {
		} catch (error) {}
	}

	async findByCategory(categoryName) {
		try {
		} catch (error) {}
	}
}

module.exports = ProductsService;
